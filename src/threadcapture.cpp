#include "threadcapture.h"

using namespace std;

ThreadCapture::ThreadCapture()
{
    status = 0;
}

ThreadCapture::~ThreadCapture()
{
    //dtor
}

void ThreadCapture::thrRun()
{
    // conectar no sql server

    // conectar na radio

    try
    {
        while (true)
        {
            // coletar recorte

            // criar fingerprint

            // mudar status sql

            // consultar na base

            // mudar status sql
        }
    }
    catch(...)
    {
        throw;
    }
}
